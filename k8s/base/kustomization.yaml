apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ""
resources:
  - namespace.yaml
  - service-account.yaml
  - role.yaml
  - role-binding.yaml
  - network-policy.yaml
  - resource-quota.yaml
  - artifact-pvc.yaml
  - gateway.yaml
  - reconciler-cronjob.yaml
# Deliberately not listed:
#   secrets.example.yaml                    - shape reference, empty values
#   network-policy-test-runner-offline.yaml - opt-in replacement policy
# The ai-test-repository-policies ConfigMap is created from
# config/repository-policies.yaml; see k8s/README.md.
